- hosts: webservers
  vars:
    hello_messages:
        - "world"
        - "web"
  roles:
    - common

- hosts: appservers
  become: true
  vars:
    hello_messages:
        - "world"
        - "app"
  roles:
    - common
